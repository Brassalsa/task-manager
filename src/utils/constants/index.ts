export enum TaskStatus {
  pending = "pending",
  completed = "completed",
  due = "due",
}

export enum TaskPriority {
  low = "low",
  normal = "normal",
  high = "high",
}

export enum UserType {
  admin = "admin",
  user = "user",
}
